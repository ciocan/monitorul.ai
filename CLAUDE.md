# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

@AGENTS.md

## Design Context

Strategic design context lives in `PRODUCT.md` (root). Visual system context lives in `DESIGN.md` (root, when generated). Both are loaded by the `impeccable` skill via `node ~/.claude/skills/impeccable/scripts/load-context.mjs`.

- **Register:** `brand` — every page IS content; design recedes so the parliamentary record leads.
- **Personality:** civic, durable, exact. Neutral archive voice; editorial-archival aesthetic family (ProPublica / ICIJ / theyworkforyou.com lane).
- **Anti-references:** SaaS marketing chrome, government-portal stiffness, news partisanship, crypto/hacker terminal cosplay.

When designing or refining UI, read `PRODUCT.md` first.

## Commands

Bun is the runtime and package manager. Next is invoked through Bun (`bun --bun next ...`) so it runs on the Bun runtime, not Node.

- `bun run dev` — start the dev server
- `bun run build` — production build
- `bun run start` — run the production build
- `bun run lint` / `bun run lint:fix` — **oxlint** (not ESLint). Config: `.oxlintrc.json`
- `bun run fmt` / `bun run fmt:check` — **oxfmt** (not Prettier). Config: `oxfmtrc.config.ts`
- `bun run typecheck` — **tsc** (not tsc-alias). Config: `tsconfig.json`

No test runner is configured.

## Architecture

- **Next.js 16 App Router** under `src/app/`. Read `node_modules/next/dist/docs/` for any framework API — see `AGENTS.md`.
- **React 19** with RSC enabled (`components.json` → `"rsc": true`).
- **Tailwind CSS v4** wired through PostCSS (`@tailwindcss/postcss`). There is no `tailwind.config.*` — theme tokens live in `src/app/globals.css` via `@theme inline { ... }` and `@custom-variant dark`. The CSS also imports `shadcn/tailwind.css` (the registry's stylesheet) and `tw-animate-css`.
- **shadcn/ui** with a non-default style: `components.json` uses `"style": "radix-lyra"` and `"baseColor": "neutral"`. Generated UI components live in `src/components/ui/` and import `Slot` from `radix-ui` (the umbrella package), not `@radix-ui/react-slot`.
- **Path alias**: `@/*` → `src/*` (see `tsconfig.json`). Aliases for `components`, `ui`, `lib`, `hooks`, `utils` are defined in `components.json`.
- **`cn` helper**: `src/lib/utils.ts` exports `cn(...inputs)` = `twMerge(clsx(inputs))`. Use it for all conditional class composition.
- **Fonts** are loaded in `src/app/layout.tsx` via `next/font/google` and exposed as CSS variables: `--font-sans` (Public Sans), `--font-display` (Source Serif 4), `--font-mono` (IBM Plex Mono). The three-voice system is documented in `DESIGN.md` §3.
- **Routes (Next.js 16 App Router)**: `/` (landing, ISR 1h, archive stats register), `/mo` (sessions register — `font-display` heading, per-year sparkbar over `sessionsIndex({ year })`, list of documents for the selected year sorted by `session_date` desc; defaults to the most recent active year, switchable via `?year=YYYY`; `?year=` is non-canonical — `alternates.canonical` always points at `/mo`; ISR 1h, JSON-LD `CollectionPage`), `/politicieni` (politicians register — same year-sparkbar shape, ranked list of top 100 politicians for the selected year via `politiciansIndex({ year, substantiveOnly })`; rankings come from `mo-speeches` aggregations because `mo-persons.stats` is empty in the corpus today. Inline `Ordonare:` toggle flips `?mode=substantive` (default — only `is_substantive: true` speeches counted) vs `?mode=all` (every intervention, including procedural turn-taking). Defaults to most recent active year, switchable via `?year=YYYY`; `?year=` and `?mode=` are non-canonical, ISR 1h, JSON-LD `CollectionPage`), `/comisii` (committees register — same year-sparkbar shape over `committeesIndex({ year })`, top 100 committees for the selected year ranked by meeting count; derived live from `mo-committee-meetings` since there is no upstream `mo-committees` index, so committees with zero indexed meetings are invisible. Defaults to most recent active year, switchable via `?year=YYYY`; `?year=` is non-canonical, ISR 1h, JSON-LD `CollectionPage`), `/mo/[year]/[part]/[issue]` (document page — Cuprins TOC + inline body that groups every child grain under each agenda item in `position_in_document` order: speeches, votes, interpellations, questions, and committee meetings (linked committee name + agenda + attendance + outcome rollup, anchored as `#comisie-<position>`). Body heading is "Stenograma" by default, "Ședințe" when the document has no agenda items but has committee meetings (typical for `committee_synthesis` issues). ISR 1h, JSON-LD `Article` + `GovernmentService`), `/mo/[year]/[part]/[issue]/pdf` (route handler — 302-redirects to a 5-min SigV4-presigned R2 URL; `force-dynamic`, hidden when `S3_*` env vars aren't set; see "PDF rule" below), `/politicieni/[slug]` (person profile — mandates + per-year speech sparkbar + 53-week activity heatmap (calendar year, defaults to year of `last_speech_date`, switchable via `?year=YYYY`; non-empty cells link to `?day=YYYY-MM-DD` which narrows the speeches list below to that single day) + recent speeches via `personPage(slug, { year, day, page })` (20 per page for year / no-filter views, exhaustive single page of up to 50 for day filter; shared `<Pagination>` component links pages with the section's `#discursuri-recente` anchor; each row shows a word count + 5-segment azure length meter via `<SpeechLengthMeter>`, derived client-side from `speech.text` — no extra ES cost; the same meter renders on `/cauta` hits), ISR 1h, JSON-LD `Person`; `?year=`, `?day=` and `?page=` are non-canonical — `alternates.canonical` always points at the bare URL), `/comisii/[committee_id]` (committee profile — header (name + kind + joint-with) + meta register (total / first / last meeting) + per-year meeting sparkbar + meeting list for the selected year via `committeePage(committee_id, { year })` (date, first agenda title, attendance ratio, agenda count, outcome counts; capped at 50 per year — denser schedules need pagination not yet shipped). Header values come from the most-recent meeting since there is no upstream `mo-committees` index. ISR 1h, JSON-LD `GovernmentOrganization`; `?year=` non-canonical), `/cauta` (speech search, `force-dynamic`, `noindex,follow`; hit rows link the speaker name to `/politicieni/<person_id>` when populated and ship a "Vezi în context →" link to the document anchor. Filter shell is a `<details>` labelled `Filtre · N` (count of active filter dimensions, multi-year counts as 1), open automatically when any filter is set. v1 filters: **year — multi-select chip row** (last 5 years + `Alt an` `<select>` for older archive; each chip is a Radix `Checkbox` so picks compose; the older-select adds the chosen year to the multi-selection on submit), chamber via shadcn `RadioGroup` rendered as chips, speaker via `<SpeakerCombobox>` (shadcn `command` + `popover`, hits `/api/search/persons?q=…&limit=10`), party-at-time via shadcn `Select` with top-12 + `Alte` optgroup (sourced from `listPartyEnumeration()` — `terms` agg over `speaker.party_group_at_time`, in-process 1h memo; field hidden when the corpus enumeration is empty), `Include intervenții procedurale` shadcn `Checkbox` (default off — substantive-only stays the default), and `Sortare` via shadcn `RadioGroup` (`Relevanță · Data ↓ · Data ↑`). Submit is the shadcn `Button` (`variant=default size=lg`). All Radix-stateful inputs key off URL-derived state (`key={…}`) so chip-removal soft navigation forces a fresh mount and stale client state can't sneak back into the next Aplică. Form is plain `method="GET"` — `<CautaFilterForm>` is a thin `'use client'` wrapper that strips empty inputs, collapses every `year` checkbox + the `year-older` select into a comma-joined `?year=2024,2007`, and uses `router.push` for soft nav. URL contract is all-English: `?q&page&from&to&year&chamber&speaker&party&procedural&sort`; `?year=` accepts a comma-separated list of years. Active filters render as a removable chip row below the panel via `<ActiveFilters>` — multi-year emits one chip per year, each a shadcn `Badge` (`variant=outline`, `asChild` over a `<Link>`) to the same URL with that single year removed; plus a `Resetează filtrele` link. Search params parsed by `parseCautaSearchParams` (Zod schema in [`src/lib/search-params.ts`](src/lib/search-params.ts)); explicit date sort bypasses RRF and falls back to BM25-only because chronological order doesn't compose with semantic rank. Per-grain pages (`/cauta/voturi`, `/cauta/interpelari`, `/cauta/comisii`) ship in v2), `/despre` (methodology page — long-form note split into two parts. **Part I — Pentru toți cititorii** is plain Romanian for citizens / journalists / researchers without a tech background: `#ce-este`, `#de-unde`, `#acoperire`, `#identitate` (plain "stable links" framing), `#cautare` (plain "exact words + similar meaning" framing), `#corectii`, `#sursa`. **Part II — Detalii tehnice** is for data journalists, researchers, devs: `#pipeline` (the 8-step register), `#identitate-tehnic` (`record_id` / `content_fingerprint` / `slug-once`), `#cautare-tehnic` (BM25 + kNN/RRF, `.folded` subfields, silent-degrade gates). The footer's Metodologie column links into `#identitate` and `#corectii` so a non-technical reader lands on the plain-language version first; cross-references inside Part I jump down to the technical detail when wanted. ISR 1h, JSON-LD `AboutPage`). Speech blocks link to `/politicieni/<speaker.person_id>` only when `person_id` is non-null — `person_id === slug` (verified against the live `mo-persons` index). The Cuprins links to in-page anchors (`#agenda-<ord>`); individual speeches are also addressable inline as `#discurs-<position_in_document>` and as standalone records at `/discurs/<slug>`. Remaining grain routes (votes, interpelations, agenda items) ship in subsequent phases. `/discurs/[slug]` (speech detail — speaker (linked to `/politicieni/<person_id>` when populated), agenda category + outcome, agenda title linked back to `#agenda-<ord>`, full body paragraph-split, word count + length meter via `<SpeechLengthMeter>`, bill refs, "În contextul ședinței →" anchor link to `#discurs-<position>`. Slug resolution via [`getSpeechBySlug`](src/lib/search.ts) — exact `term` on `slug` keyword; on miss, a `*-<short_id>` `wildcard` fallback bounded by `terminate_after: 1` so renamed slug-prefixes still resolve to the canonical record. When the requested slug differs from `speech.slug` the page issues `permanentRedirect(speech.url_path)` (308). Substantive speeches are `index, follow`; procedural turns drop to `noindex, follow`. JSON-LD `Quotation` + `Person` + `isPartOf` parent `Article`, ISR 1h.).
- **404s**: `src/app/not-found.tsx` is the catch-all (unmatched URLs + any `notFound()` call without a closer match) — it ships search + entry-point wayfinding. Scoped `not-found.tsx` lives next to each dynamic route (`/mo/[year]/[part]/[issue]`, `/politicieni/[slug]`, `/comisii/[committee_id]`, `/discurs/[slug]`) for in-context messaging when a specific record is missing. They share the same eyebrow / headline / paragraph / button structure; copy stays in the civic-archive register.

**UI rule (non-negotiable):** before writing any new JSX for an interactive primitive, check `src/components/ui/` and the `@shadcn` registry first. If a shadcn primitive exists, use it — even if you have to wrap it. Hand-rolling is reserved for **signature components** that have no shadcn analogue (e.g. `Dateline`, `StatsRegister`).

- **Already installed** (`src/components/ui/`): `button`, `input`, `input-group` (+ `InputGroupAddon` / `InputGroupInput` / `InputGroupButton` / `InputGroupText`), `kbd` (+ `KbdGroup`), `textarea`, `empty` (reserved for the search-results no-matches page; the document-page agenda absence stays as a quiet inline note, not an `Empty` block — see DESIGN.md "no illustrations" Don't), `command`, `popover`, `dialog`, `select`, `checkbox`, `radio-group`, `badge`, `label`, `separator`, `field`.
- **Likely needed soon** — install with `bunx --bun shadcn@latest add <name>`: `card`, `skeleton`, `table`, `tabs`, `dropdown-menu`, `tooltip`, `breadcrumb`, `pagination`.
- **Workflow:** run the `add` command. **When prompted to overwrite an existing file, decline (`n`)** — the existing file in this repo has been adjusted to project conventions and overwriting destroys those edits. The CLI may still install transitive deps you didn't ask for (e.g. `input-group` pulls in `textarea`); that's fine.
- **Style:** `components.json` pins `"style": "radix-lyra"` and `"baseColor": "neutral"`. Generated components import `Slot` from `radix-ui` (the umbrella package), not `@radix-ui/react-slot`. Don't switch styles.
- **When NOT to use a shadcn primitive:** purely typographic / editorial constructs that _are_ the design system's signature voice — `Dateline`, the landing's typeset register, mono uppercase labels (`label-mono` is a CSS class, not a component). These belong in `src/components/`, not in `src/components/ui/`.
- **Tokens:** every shadcn primitive must read from the project's tokens (`paper-99`, `paper-96`, `paper-91`, `ink-16`, `ink-30`, `ink-45`, `azure-3`, `alert-civic`) via the existing semantic aliases (`bg-background`, `border-input`, `text-foreground`, etc.) which `globals.css` maps onto our scale. Don't hardcode hex / oklch values inside components.

**ES rule:** every read goes through [`src/lib/search.ts`](src/lib/search.ts). No direct `@elastic/elasticsearch` calls in pages or route handlers. Function set mirrors the upstream `monitorul_ii.elasticsearch.queries` module and inherits its caps (`pageSize ≤ 50`, `isSubstantive=true` default). Grain shapes live in [`src/lib/types.ts`](src/lib/types.ts); the deeper field-by-field reference is [`docs/architecture.md`](docs/architecture.md). Self-hosted ES with self-signed cert is the default — `ES_VERIFY_CERTS=1` only when the cluster has a public chain.

**MCP rule:** the public MCP server has two URLs on the same Next.js app — `/mcp` is the human-facing presentation page (`src/app/mcp/page.tsx`, ISR 1h, JSON-LD `WebAPI`); `/mcp/server` is the streamable-HTTP endpoint AI clients connect to (`src/app/mcp/server/route.ts`), wrapped in Better Auth's `withMcpAuth(auth, …)` so every request carries a verified bearer token. Discovery routes are public at `/.well-known/oauth-protected-resource` (RFC 9728) and `/.well-known/oauth-authorization-server` (RFC 8414); the auth catch-all is at `/api/auth/[...all]/route.ts`. The handler passes `streamableHttpEndpoint: "/mcp/server"` to `mcp-handler` so the dispatcher's pathname-comparison agrees with the file location — no rewrite needed. The route registers 16 Zod-typed tools that 1:1 wrap functions in `src/lib/search.ts`; the route file should never grow ES query bodies. Search-shape hits go through the cataloguer in [`src/lib/mcp-adapters.ts`](src/lib/mcp-adapters.ts) (240ch excerpt + pinned absolute URL via `AsyncLocalStorage`-scoped per-request origin); lookup tools return raw lib shapes. `describe_corpus` data lives in [`src/lib/describe-corpus.ts`](src/lib/describe-corpus.ts) — chamber/topic/reference enumerations are aggregated live so the LLM reads the truth of the corpus on first call. Rate limits in [`src/lib/ratelimit.ts`](src/lib/ratelimit.ts): Upstash sliding window across two axes (per-IP + per-user), two tiers each (30/min general, 20/min RRF/kNN heavy); both must clear or 429. The limiter no-ops when `UPSTASH_REDIS_REST_URL` / `_TOKEN` are unset. Deep dive: [`docs/mcp.md`](docs/mcp.md).

**Auth rule:** Better Auth (`src/lib/auth.ts`) backs the MCP OAuth dance + the web sign-in flow. Single Google social provider for V1; DCR + PKCE; tokens persisted in Neon Postgres via the Drizzle adapter (`src/lib/db/client.ts`, schema generated by `bunx better-auth generate`). User-facing surfaces under `/cont/*`: `/cont/intra` (sign-in), `/cont` (authorized-clients list + revoke + sign-out), `/cont/consimt` (themed consent screen). Server actions at `src/app/cont/actions.ts` and `src/app/cont/consimt/actions.ts` go through `auth.api.*` (not the client SDK) so cookies live in request headers, not `document.cookie`. The MCP route stamps `session.userId` into `requestContext` so every `mo_query_log` row gets attribution; the autocomplete API and RSC pages leave `user_id: null`. Discovery + sign-in flow: [`docs/mcp.md`](docs/mcp.md).

**Search rule:** `searchSpeeches` defaults to `rankFusion: "rrf"` — hybrid BM25 + kNN over `enrichments.embedding` (1024-dim BGE-M3), fused client-side via Reciprocal Rank Fusion (Σ 1/(60 + rank)). The BM25 leg fans out across `SPEECH_SEARCH_FIELDS` — main fields (`text^2` / `agenda_title^1.5` / `speaker.name_search`) paired with their `.folded` subfields at lower boosts so a query like `sosoaca` matches indexed `șoșoacă` via `text.folded` while diacritic-correct queries still rank exact matches first; the highlighter uses `matched_fields` so snippets render either way. Vector comes from the embed provider selected by `EMBED_PROVIDER` (see [`src/lib/embed.ts`](src/lib/embed.ts)): `local` calls the FastAPI service at `EMBED_URL` (monitorul-ii box, dev default); `cloud` calls any OpenAI-compatible `/v1/embeddings` endpoint at `EMBED_CLOUD_URL` with `Authorization: Bearer $EMBED_CLOUD_TOKEN` for the `EMBED_CLOUD_MODEL` model (default `bge-m3`; used on Vercel). Toggle by flipping `EMBED_PROVIDER` in `.env.local`; both sets of creds can stay populated. Silent BM25 fallback when: query is empty, embed service unreachable, user is past the fusion pool depth (~10 pages), or BM25 returns zero hits (gates out kNN hallucination on nonsense queries — pre-fix the no-diacritic case `sosoaca` would hit this gate; post-fix `text.folded` rescues it). Mode is surfaced on `/cauta` as a `Hibrid` / `BM25` chip next to the took-ms timing. Native ES `retrievers.rrf` is Platinum-licensed and not used.

**Env rule:** read env vars from [`src/env.ts`](src/env.ts) (validated by `@t3-oss/env-nextjs` + Zod). Never use `process.env.*` directly in app code. To add a new var: declare it in `src/env.ts` (server / client / shared), add it to `.env.example`, and import `env` where needed. `next.config.ts` imports the module so `next dev` / `next build` fail fast on missing or malformed values; `SKIP_ENV_VALIDATION=1` bypasses for lint-only CI.

**PDF rule:** original PDFs live in a PRIVATE Cloudflare R2 bucket (S3-compatible, see `S3_*` env vars). The bucket is never exposed to the browser — [`src/lib/pdf.ts`](src/lib/pdf.ts) derives the bucket key from the indexed `MoDocument` (`{published}_MO-P{part}-{issue}-{year}.pdf`, the shape minted upstream by the monitorul-ii scraper) and uses `aws4fetch` to mint short-lived (5 min) SigV4-presigned GET URLs server-side. The `/mo/[year]/[part]/[issue]/pdf` route handler 302-redirects to that signed URL; signing keys never reach the client. The PDF link on document pages is hidden when the bucket isn't configured or the document has no `published` date. Per-request signing is intentional (caching the redirect would also cache an expiring signature); the cost is microseconds.

**Discourse-UI rule:** the four-prompt analysis layer (Hawkins populism / V-Party anti-pluralism / DQI / voice; indexed under `mo-speeches.enrichments.discourse.*` for substantive speeches roughly from late 2019 onwards) surfaces on four pages: `/discurs/[slug]` (inline marker overlay + right-rail side panel via `prepareOverlay` in [`src/lib/discourse-markers.ts`](src/lib/discourse-markers.ts)), `/politicieni/[slug]` (framework tab strip + per-month aggregate band + speech-dot scatter via `personDiscourseTrajectory`), `/mo/[year]/[part]/[issue]` (top stat strip + Cuprins per-agenda chip + inline speaker chip via `summarizeDocumentDiscourse` in [`src/lib/document-discourse.ts`](src/lib/document-discourse.ts), no extra ES round-trip — derived from `listDocumentChildren`), and `/statistici` (four panels: time series, H×V cross-tab, top politicians with Wilson 95% CI, marker treemap). Filter chips are a shared shape across all four surfaces: voice (`?voice=all` to include quoted/negated/apophasis voices, default = `speaker_first_person`) and confidence (`?conf=07` to filter framework_confidence ≥ 0.7). Both URL params are non-canonical. Schema rule: speech-act labelling, never personality attribution — methodology + model disclaimer rendered on every aggregating surface. Marker-kind labels live in [`src/lib/discourse-copy.ts`](src/lib/discourse-copy.ts) (Romanian translations for every Hawkins / V-Party / DQI marker kind + voice value). Charts are hand-rolled SVG/CSS in [`src/components/discourse/`](src/components/discourse/) — no chart library. Design record: [`docs/_session-handoff-2026-05-10-discourse-ui.md`](docs/_session-handoff-2026-05-10-discourse-ui.md). Upstream schema: `monitorul/docs/discourse-analysis-schema.md` (Q2 = personality-attribution ban, Q5 = voice keystone) and `monitorul/docs/canonical-queries.md` (mandatory disclaimer text).

## Code Quality

**After meaningful feature changes, update `README.md` (user-facing), `CLAUDE.md` (this file but keep it concise), and `docs/architecture.md` (deep dives).** "Meaningful" = a new feature, a new behavior or default, a new module / page / route, a new external dependency, or anything a future user/agent would otherwise have to read the diff to discover. Trivial bug fixes and pure refactors don't need a doc update. **Keep CLAUDE.md scannable** — push detailed mechanics into `docs/architecture.md` and link from here.

```bash
bun run fmt
bun run lint
bun run typecheck
```

Fix every issue surfaced by any of the three — including oxlint warnings in files you authored. Do not leave a task in a state where `bun run lint` or `bun run typecheck` reports errors.

## Release flow

Releases are managed by **release-please** via `.github/workflows/release-please.yml` (config: `release-please-config.json`, manifest: `.release-please-manifest.json`). Use Conventional Commits so the action can compute version bumps.
